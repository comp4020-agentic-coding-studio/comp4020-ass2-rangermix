# Shared implementation brief

Read `CLAUDE.md`, `docs/course-design.md`, `docs/plans/2026-09-18-course-consistency.md`, and the previous reports in `docs/reviews/2026-09-18-applied/reports/`. User has authorized fixing all issues with judgment, explicitly accepts either run/decline in week 6 and prediction-only counterfactuals in week 2. Do not ask for approval again. Preserve satire and protected text.

Timing: lecture 14:05 start, 14:55–15:05 fixed break, core around 15:40, optional discussion ends by15:55. Root owns shared timetable, estimator, tests and break-slide insertion. Estimator will use 2 minutes for every ordinary slide (including quote/table/diagram/impact), 5 seconds for a banner or `{/\* timing: transition \*/}` marker, and explicit `##/### In the room … (N min)` headings for activities. No added note time. Root will add a break slide, excluded from core time. Aim at roughly 85 core minutes (75–95 planning band); add substantive worked examples/practice, not arbitrary labels to buy time. Timed headings must contain concrete instructions/prompts and workable phases. Don't turn a regular slide into a long activity by changing only its label.

Every lecture/deck ends with one optional open-ended discussion question. Use `## If time remains` on the last deck slide, a literal MDX comment `{/* timing: discussion */}`, the question, and `Optional discussion. Finish by 15:55.` No required self-check depends on participating. Mirror under a final level-3 subsection on the lecture page where possible to avoid shifting numbered sections. The five-second transition marker uses the same literal MDX comment syntax, without backslashes. The week 10 snap-back text remains verbatim; root can add the separate timing marker outside that text.

Labs use 50 minutes of actual work: all run sheets start `0:00` and end `0:50`, with contiguous rows. Add `Times below run from the 14:05 start; finish at 14:55.` beneath the run-sheet heading. Preserve required learning work by shortening optional reporting, making counts realistic, completing role rotations and using prepared fictional examples when real observations are absent. If a particular lab really needs an extended booking, write the justification to your result and tell root before changing shared times; don't pretend tasks fit by merely relabelling their totals.

Scope boundaries: agents edit only assigned files and their own result file. Do not edit `PROCESS.md`, the existing patch, historical editorial/archive/review files, shared tests, root-owned timetable/design/estimator files, or git state. Root runs integration checks and commits. Cite changes/findings and any remaining dependency in the result file. Agent messages should point to files; useful state belongs there.
