# Integration notes

Root's checks, closed during final integration on 18–19 September 2026.
The agent result files describe their handoff state; the README and
`timing.json` record the final combined state.

- Week 9's pass demonstration now has the tutor read both fictional roles;
  no student has to perform a real refusal.
- The journal's Friday follow-up asks “Are Thursdays usually good for you?”
  The separate Friday request supplies a genuine second observed instance.
- Week 9's diagram fits at 844×390. Its page/deck call-site captions no longer
  repeat the lower-level/pass exception already supplied by the component's
  visible caption and accessible description.
- Week 6's five-field ledger accepts explicit private placeholders and labelled
  fictional practice. Neither a real masking row nor a switch is compulsory.
  Fictional estimates cannot become actual Field Journal evidence.
- The independent timing review reproduced a displaced-break gap. Root added
  a failing fixture, the 45–55-minute placement gate and a check on every real
  deck. All thirteen estimator tests pass. Text and JSON CLI modes agree.
- The CLI's entry guard now resolves symlinks and allows safe stdin imports;
  both cases have passing regression coverage.
- All twelve break slides are inserted at useful boundaries. Week 12's model
  cautions and reading starting points precede E-6, giving it a 47.1-minute
  boundary. The week 10 snap-back is a five-second transition. Its protected
  words and order remain unchanged, as do the lecture's.
- Week 8's email already contains a draft, so Monday now means reviewing that
  draft. The page, deck and clause-six note agree.
- The timetable's rendered word joins were reproduced and fixed with explicit
  whitespace around interpolated times; the regression passes.
- The whole-site label test mistakenly rejected “study session” in a fictional
  example. It now checks course titles, navigation and meeting labels. The
  individual Lab headings remain independently checked.
- The landscape check found clipped instructions on week 4's study-comparison
  activity. A separate ordinary slide introduces the table in two minutes;
  the matching page/deck activity is now eight minutes. Core time is unchanged.
  The weekly handoff's activity-minute total predates this integration change.
- Direct deck visits exposed a missing favicon and a root-path 404. The deck
  integration now links to a stable SVG endpoint that reuses the existing
  Slop crest. A rendered regression checks the asset and all twelve icon links.

The initial combined schedule run had 33 failing cases. Final `pnpm check`
passes all 464 tests across twelve files, with a fresh 125-page build.
Final local browser results are in the README. Remote CI is separate.
